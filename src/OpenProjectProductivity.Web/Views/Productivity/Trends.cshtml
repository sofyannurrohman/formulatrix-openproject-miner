@* @model IEnumerable<OpenProductivity.Web.ViewModels.TrendVm>

<h2>Team Trends</h2>

<canvas id="trendChart"></canvas>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        const ctx = document.getElementById('trendChart');
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(t => t.Week))),
                datasets: [
                    {
                        label: 'Completed Tasks',
                        data: @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(t => t.CompletedTasks))),
                        borderColor: 'blue',
                        yAxisID: 'y'
                    },
                    {
                        label: 'Avg Duration (days)',
                        data: @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(t => t.AvgDuration))),
                        borderColor: 'green',
                        yAxisID: 'y1'
                    }
                ]
            },
            options: {
                scales: {
                    y: { type: 'linear', position: 'left' },
                    y1: { type: 'linear', position: 'right' }
                }
            }
        });
    </script>
} *@
