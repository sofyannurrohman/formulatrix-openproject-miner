@* @model OpenProductivity.Web.ViewModels.MemberDetailVm

<h2>Details for @Model.MemberName</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Work Package</th>
            <th>Status History</th>
            <th>Duration (days)</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var task in Model.Tasks)
    {
        <tr>
            <td>@task.Id</td>
            <td>
                @foreach (var t in task.StatusHistory)
                {
                    <div>@t.Timestamp: @t.FromStatus → @t.ToStatus</div>
                }
            </td>
            <td>@task.Duration.ToString("0.00")</td>
        </tr>
    }
    </tbody>
</table>

<canvas id="durationChart"></canvas>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        const ctx = document.getElementById('durationChart');
        new Chart(ctx, {
            type: 'bar',
            data: {
                labels: @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Tasks.Select(t => t.Id))),
                datasets: [{
                    label: 'Task Duration (days)',
                    data: @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Tasks.Select(t => t.Duration))),
                }]
            }
        });
    </script>
} *@
